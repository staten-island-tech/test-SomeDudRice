def input (age, resident, membership):
    if (age <= 12 or age >=65 or resident = True or membership = True)
        print ("discount allowed")
    
    else
     print("we dont accept poor ppl sorry")
